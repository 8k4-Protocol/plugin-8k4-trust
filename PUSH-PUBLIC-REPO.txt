# plugin-8k4-trust public repo bootstrap
# Run from: /root/.openclaw/workspace/projects/plugin-8k4-trust

# 1) sanity check
pwd
git status --short --branch
git log --oneline -n 5

# 2) rename default branch if you want main locally
# (optional if you'll open a PR from feature/initial-standalone-extract)
# git branch -M main

# 3) add the new GitHub repo remote
# replace with the real repo URL if needed
git remote add origin git@github.com-8k4org:8k4-Protocol/plugin-8k4-trust.git

# 4) push the current extraction branch
git push -u origin feature/initial-standalone-extract

# 5) after the repo exists, verify remote state
git remote -v

# 6) optional: if you want main to contain the extracted history directly
# git checkout -b main
# git push -u origin main

# 7) CI/local validation before publish
npm install
npm run build
npm run test

# 8) when ready to publish
# npm login
# npm publish --access public
